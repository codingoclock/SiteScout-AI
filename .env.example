# Copy to .env and adjust as needed. Config (agent/chat/config.py) is the
# only place these variables are read — see its module docstring.

MODEL_TYPE=openai
OPENAI_MODEL=gpt-3.5-turbo
STORE_TYPE=redis
STORE_HOST=localhost
STORE_PORT=6379
NAMESPACE=default
INPUT_DIR=./data
CHUNK_SIZE=1024
